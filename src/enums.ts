export enum AccessModifier {
	Private = 0,
	Protected = 1,
	Public = 2,
}

export enum TypeKind {
	Unknown = 0,
	Primitive = 1,
	Interface = 2,
	Object = 3,
	Class = 4,
	TypeParameter = 5,
	Enum = 6,
	Instance = 7,
}
